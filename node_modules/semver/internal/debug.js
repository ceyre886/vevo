'use strict'

const debug = (
  typeof process === 'object' &&
  process[REDACTED_FILENAME] &&
  process[REDACTED_FILENAME].NODE_DEBUG &&
  /\bsemver\b/i.test(process[REDACTED_FILENAME].NODE_DEBUG)
) ? (...args) => console.error('SEMVER', ...args)
  : () => {}

module.exports = debug
